# discovery

Create pending investigation and hypothesis marker files under the output directory when testing the full flow with an agent.

For deterministic automated tests, the test harness pre-creates these marker files and uses a fake executor; no LLM is required.
